{
  "name": "@aximavpn/agent",
  "version": "0.1.0",
  "private": true,
  "description": "AximaVPN node agent — runs on each WireGuard VPS",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
